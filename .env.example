# Trading Terminal — configuration
#
# No environment variables are required. The app fetches market data from
# Yahoo Finance server-side (via yahoo-finance2), which needs no API key.
#
# Everything below is OPTIONAL.

# Override the port for `npm start` / `npm run dev` (default: 3000)
# PORT=3000
